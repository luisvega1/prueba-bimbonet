import { FilterDestacadosPipe } from './filter-destacados.pipe';

describe('FilterDestacadosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDestacadosPipe();
    expect(pipe).toBeTruthy();
  });
});
